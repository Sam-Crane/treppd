# Dev/test/lint tools — NOT installed in the Docker production image.
# Install locally or in CI with: pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==6.0.0
ruff==0.8.0
mypy==1.13.0
